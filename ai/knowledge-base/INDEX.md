# Knowledge Base Index

## tools/

- [dotnet-build-quirks.md](tools/dotnet-build-quirks.md) — testhost runtime roll-forward workaround, `.slnx` vs `.sln`, IDE0008 var rule as build error, NuGet packages that don't exist (MCAP, ParquetSharp 16.0.0).
